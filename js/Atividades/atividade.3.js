// npm install readline-sync
const readlineSync = require('readline-sync');

let numeroA = readlineSync.questionInt('Digite um número: ');
let numeroB = readlineSync.questionInt('Digite outro número: ');

if (numeroA = numeroB) {
    numeroA + numeroB;
    console.log(`A soma dos números é: ${numeroA + numeroB}`);
}
else if (numeroA =+ numeroB) {
      valorA * valorB;
      console.log(`A multiplicação dos números é: ${numeroA * numeroB}`);
}  
